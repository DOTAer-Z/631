# ---------------------------------------------------------------------------
# 生产/部署用依赖（docker 镜像使用）
# 自 2026-06-14 起将本地 embedding 模型（bge-small-zh-v1.5）打包进镜像，
# 因此重新加入 sentence-transformers + torch + faiss-cpu。
# 模型权重已在 backend 镜像构建时通过 COPY models/bge-small-zh-v1.5 → /models 嵌入。
# ---------------------------------------------------------------------------
fastapi==0.111.0
alembic==1.13.1
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
pydantic-settings==2.2.1
chromadb==0.5.3
openai==1.30.0
dashscope==1.19.0
redis[hiredis]==5.0.4
python-multipart==0.0.9
httpx==0.27.0
pymongo==4.6.1
numpy==1.26.4

# —— 本地 Embedding（bge-small-zh-v1.5）—————————————
# torch CPU-only 轮子，避免拉 ~2GB CUDA 依赖
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
sentence-transformers==2.7.0
faiss-cpu==1.8.0
cryptography==42.0.8
safetensors==0.7.0
